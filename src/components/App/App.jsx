import { Provider } from 'react-redux';
import './App.css';
import { BrowserRouter } from 'react-router-dom';

const App = () => {
  return (
    <Provider>
      <BrowserRouter>
      <Routes>

      </Routes>
      </BrowserRouter>
    </Provider>
  );
}

export default App;
